<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(to bottom, #f8f9fa, #e9ecef);
      color: #333;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    header {
      background: linear-gradient(to right, #007BFF, #0056b3);
      padding: 20px;
      color: white;
      text-align: center;
      font-size: 1.5rem;
      font-weight: bold;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    header nav {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 10px;
    }

    header nav a {
      color: white;
      text-decoration: none;
      font-size: 1rem;
      padding: 10px 15px;
      border-radius: 5px;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    header nav a:hover {
      background-color: rgba(255, 255, 255, 0.2);
      transform: scale(1.1);
    }

    main {
      max-width: 1200px;
      margin: 30px auto;
      padding: 20px;
      background: white;
      border-radius: 15px;
      box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
    }

    .confirmation {
      text-align: center;
      margin-bottom: 30px;
      padding: 20px;
      background: #e9f7ef;
      border: 1px solid #28a745;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      animation: fadeIn 0.5s ease;
    }

    .confirmation h1 {
      color: #28a745;
      margin-bottom: 15px;
    }

    .confirmation p {
      font-size: 1.1rem;
      margin-bottom: 10px;
      color: #555;
    }

    .confirmation a {
      display: inline-block;
      margin-top: 15px;
      padding: 10px 20px;
      font-size: 1rem;
      color: white;
      background-color: #007BFF;
      border-radius: 5px;
      text-decoration: none;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .confirmation a:hover {
      background-color: #0056b3;
      transform: scale(1.05);
    }

    h1 {
      text-align: center;
      margin-bottom: 20px;
      font-size: 2.5rem;
      color: #007BFF;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
      border-radius: 10px;
      overflow: hidden;
    }

    table thead {
      background-color: #007BFF;
      color: white;
    }

    table th,
    table td {
      padding: 15px;
      text-align: left;
    }

    table th {
      font-size: 1.1rem;
    }

    table tbody tr:nth-child(odd) {
      background-color: #f9f9f9;
    }

    table tbody tr:hover {
      background-color: #eef2f3;
      cursor: pointer;
    }

    table td {
      font-size: 1rem;
      color: #555;
    }

    .pagination {
      margin-top: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 10px;
    }

    .pagination a {
      padding: 10px 15px;
      font-size: 1rem;
      color: white;
      background-color: #007BFF;
      border-radius: 5px;
      text-decoration: none;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .pagination a:hover {
      background-color: #0056b3;
      transform: scale(1.1);
    }

    footer {
      margin-top: auto;
      background: linear-gradient(to right, #333, #444);
      color: white;
      text-align: center;
      padding: 15px;
      font-size: 0.9rem;
      box-shadow: 0 -4px 6px rgba(0, 0, 0, 0.1);
    }

    footer p {
      margin: 0;
    }

    @media (max-width: 768px) {
      table th,
      table td {
        font-size: 0.9rem;
        padding: 10px;
      }

      header nav {
        flex-direction: column;
        gap: 10px;
      }

      h1 {
        font-size: 2rem;
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>
  <header>
    {{title}}
    <nav>
      <a href="/">Home</a>
      <a href="/search">Search AirBnBs</a>
      <a href="/login">Login</a>
    </nav>
  </header>

  <main>
    {{#if updated}}
    <div class="confirmation">
      <h1>Listing Updated Successfully</h1>
      <p><strong>Name:</strong> {{updated.name}}</p>
      <p><strong>Price:</strong> ${{updated.price}}</p>
      <p><strong>Location:</strong> {{updated.location}}</p>
      <p><strong>Street:</strong> {{updated.address.street}}</p>
      <p><strong>Suburb:</strong> {{updated.address.suburb}}</p>
      <p><strong>Country:</strong> {{updated.address.country}}</p>
      <a href="/all-data">View All Listings</a>
    </div>
    {{/if}}

    {{#if airbnbs.length}}
    <h1>Search Results</h1>
    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Price</th>
          <th>Minimum Nights</th>
          <th>Address</th>
        </tr>
      </thead>
      <tbody>
        {{#each airbnbs}}
        <tr>
          <td>{{this.name}}</td>
          <td>${{this.price}}</td>
          <td>{{this.minimum_nights}}</td>
          <td>{{this.address.street}}, {{this.address.country}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
    <div class="pagination">
      {{#if hasPrevPage}}
      <a href="/?page={{prevPage}}&perPage={{perPage}}">Previous</a>
      {{/if}}
      {{#if hasNextPage}}
      <a href="/?page={{nextPage}}&perPage={{perPage}}">Next</a>
      {{/if}}
    </div>
    {{else}}
    <p>No listings available.</p>
    {{/if}}
  </main>

  <footer>
    <p>&copy; 2024 AirBnB Finder. All Rights Reserved.</p>
  </footer>
</body>
</html>
