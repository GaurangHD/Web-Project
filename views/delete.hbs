<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete AirBnB Listing</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(to right, #f8f9fa, #e9ecef);
            color: #333;
            line-height: 1.6;
            text-align: center;
            padding: 20px;
        }

        .container {
            background: white;
            border-radius: 12px;
            padding: 30px;
            max-width: 600px;
            margin: 50px auto;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        }

        h1 {
            font-size: 2rem;
            color: #dc3545;
            margin-bottom: 20px;
        }

        p {
            font-size: 1.1rem;
            margin-bottom: 30px;
        }

        .btn-group {
            display: flex;
            justify-content: center;
            gap: 20px;
        }

        .btn {
            padding: 12px 20px;
            font-size: 1rem;
            font-weight: bold;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn-cancel {
            background: #007BFF;
            color: white;
        }

        .btn-cancel:hover {
            background: #0056b3;
        }

        .btn-delete {
            background: #dc3545;
            color: white;
        }

        .btn-delete:hover {
            background: #c82333;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Delete Listing</h1>
        <p>Are you sure you want to delete this listing? This action cannot be undone.</p>
        <div class="btn-group">
            <form action="/delete/{{id}}" method="POST"
                onsubmit="return confirm('Are you sure you want to delete this listing?');">
                <button type="submit" class="btn btn-delete" aria-label="Confirm Deletion">Yes, Delete</button>
            </form>
            <a href="/all-data" class="btn btn-cancel" aria-label="Cancel Deletion">Cancel</a>
        </div>
    </div>
</body>

</html>