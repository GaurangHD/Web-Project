<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All AirBnB Listings</title>
  <link rel="stylesheet" href="/public/css/style.css">

  <style>
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Roboto', sans-serif;
      line-height: 1.6;
      background: linear-gradient(to right, #eef2f3, #8e9eab);
      color: #333;
    }

    header {
      background: linear-gradient(to right, #007BFF, #0056b3);
      color: white;
      padding: 20px;
      text-align: center;
      position: sticky;
      top: 0;
      z-index: 100;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    header nav {
      display: flex;
      justify-content: center;
      gap: 20px;
    }

    header nav a {
      color: white;
      text-decoration: none;
      font-size: 1rem;
      font-weight: bold;
      padding: 10px 15px;
      border-radius: 5px;
      transition: background-color 0.3s, transform 0.2s;
    }

    header nav a:hover {
      background-color: rgba(255, 255, 255, 0.3);
      transform: scale(1.05);
    }

    main {
      max-width: 1200px;
      margin: 30px auto;
      padding: 20px;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 25px;
    }

    .card {
      background: white;
      border-radius: 15px;
      box-shadow: 0 6px 10px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .card:hover {
      transform: translateY(-10px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    }

    .card img {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }

    .card-body {
      padding: 20px;
    }

    .card-title {
      font-size: 1.5rem;
      color: #007BFF;
      font-weight: bold;
      margin-bottom: 10px;
    }

    .card-price {
      font-size: 1.2rem;
      color: #28a745;
      margin-bottom: 10px;
    }

    .card-address {
      font-size: 0.9rem;
      color: #555;
      margin-bottom: 15px;
    }

    .card-amenities {
      font-size: 0.9rem;
      color: #333;
      margin-bottom: 15px;
    }

    .card-actions {
      display: flex;
      justify-content: space-around;
      align-items: center;
      padding: 15px;
      background: #f9f9f9;
      border-top: 1px solid #ddd;
    }

    .card-actions .card-btn {
      padding: 10px 20px;
      font-size: 1rem;
      font-weight: bold;
      color: white;
      background-color: #007BFF;
      border: none;
      border-radius: 5px;
      text-align: center;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.2s;
      text-decoration: none;
    }

    .card-btn {
      padding: 10px 20px;
      font-size: 1rem;
      font-weight: bold;
      color: white;
      background-color: #007BFF;
      border: none;
      border-radius: 5px;
      text-align: center;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.2s;
    }

    .card-btn:hover {
      background-color: #0056b3;
      transform: scale(1.05);
    }

    .card-actions {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px;
      background: #f9f9f9;
      border-top: 1px solid #ddd;
    }

    .card-actions .card-btn:hover {
      background-color: #0056b3;
      transform: scale(1.05);
    }

    footer {
      text-align: center;
      padding: 20px;
      background: linear-gradient(to right, #007BFF, #0056b3);
      color: white;
      font-size: 0.9rem;
      position: relative;
      bottom: 0;
      width: 100%;
      margin-top: 30px;
    }

    .pagination {
      margin: 20px auto;
      text-align: center;
      font-size: 1rem;
      font-weight: bold;
    }

    .pagination-buttons {
      display: inline-flex;
      gap: 10px;
    }

    .pagination a {
      padding: 10px 20px;
      color: white;
      background-color: #007BFF;
      border-radius: 5px;
      text-decoration: none;
      transition: background-color 0.3s, transform 0.2s;
    }

    .pagination a:hover {
      background-color: #0056b3;
      transform: scale(1.1);
    }

    .pagination-info {
      margin-top: 10px;
      color: #555;
      font-size: 0.9rem;
    }

    @media (max-width: 768px) {
      header nav {
        flex-direction: column;
        align-items: center;
      }

      .pagination-buttons {
        flex-wrap: wrap;
      }
    }
  </style>
</head>

<body>
  <header>
    <nav>
      <a href="/">Home</a> <!-- Route for homepage -->
      <a href="/search">Search AirBnBs</a> <!-- Route for search page -->
      <a href="/add-new">Add New Listing</a> <!-- Route for adding a new listing -->
      <a href="/login">Login</a> <!-- Route for login page -->
    </nav>
  </header>

  <main>
    {{#if airbnbs.length}}
    {{#each airbnbs}}
    <div class="card">
      {{#if this.images.picture_url}}
      <img src="{{this.images.picture_url}}" alt="{{this.name}}">
      {{else}}
      <div
        style="display: flex; justify-content: center; align-items: center; height: 200px; background-color: #f4f4f4; color: #888; font-size: 1rem;">
        Image preview is not available.
      </div>
      {{/if}}
      <div class="card-body">
        <h3 class="card-title">{{this.name}}</h3>
        <p class="card-price">$ {{this.price}} / night</p>
        <p class="card-address">{{this.address.street}}, {{this.address.suburb}}, {{this.address.country}}</p>
        <p class="card-amenities">Amenities: {{#each this.amenities}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}</p>
      </div>
      <div class="card-actions">
        <form action="/edit/{{this._id}}" method="GET" style="margin: 0;">
          <button type="submit" class="card-btn">Edit</button>
        </form>
        <form action="/delete/{{this._id}}" method="POST" style="margin: 0;">
          <button type="submit" class="card-btn"
            onclick="return confirm('Are you sure you want to delete this listing?');">Delete</button>
        </form>
      </div>
    </div>
    {{/each}}
    {{else}}
    <p style="text-align: center; font-size: 1.2rem; color: #555;">No listings available.</p>
    {{/if}}
  </main>

  <div class="pagination">
    <div class="pagination-buttons">
      {{#if hasPrevPage}}
      <a href="/?page={{prevPage}}&perPage={{perPage}}">Previous</a>
      {{/if}}

      {{#if hasNextPage}}
      <a href="/?page={{nextPage}}&perPage={{perPage}}">Next</a>
      {{/if}}
    </div>
    <p class="pagination-info">
      Showing page {{currentPage}} of {{totalPages}} (Total Listings: {{totalRecords}})
    </p>
  </div>

  <footer>
    <p>&copy; 2024 AirBnB Listings | Powered by Modern UI</p>
  </footer>
</body>

</html>